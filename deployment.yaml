apiVersion: v1
kind: Service
metadata:
  name: kac-demo
  labels:
    app: kac-demo
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: kac-demo
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: kac-demo
  labels:
    app: kac-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kac-demo
  template:
    metadata:
      # annotations:
      #   sidecar.istio.io/inject: "false"
      labels:
        app: kac-demo
    spec:
      containers:
        # replace the repository URL with your own repository (e.g. {DockerID}/http-db-service:0.0.x for Docker Hub).
        - image: jcawley5/kac-demo:latest
          imagePullPolicy: Always
          name: kac-demo
          ports:
            - name: http
              containerPort: 8000
---
apiVersion: gateway.kyma-project.io/v1alpha2
kind: Api
metadata:
  name: kac-demo
  labels:
    app: kac-demo
spec:
  hostname: kac-demo
  service:
    name: kac-demo
    port: 80
